// import 'package:ash_binance_api_core/ash_binance_api_core.dart';
// import 'package:ash_binance_api_spot/ash_binance_api_spot.dart';

void main() async {
  // // 1. Initialize the HTTP client
  // final httpClient = BinanceHttpClient();
  //
  // // 2. Initialize the Spot Market Data client
  // final marketClient = BinanceSpotMarketDataClient(httpClient);
  //
  // // 3. Get the latest price for BTCUSDT
  // print('Fetching BTCUSDT price...');
  // final result = await marketClient.getPrice(const Symbol('BTCUSDT'));
  //
  // result.match(
  //   (price) => print('BTCUSDT Price: ${price.price}'),
  //   (error) => print('Error: ${error.message}'),
  // );
  //
  // // 4. Close the client
  // httpClient.close();
}
