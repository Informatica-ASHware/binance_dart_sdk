name: binance_core
description: Primitivas y transporte base para binance_dart_sdk.
version: 0.1.0
publish_to: none # Cambiar cuando esté listo para pub.dev

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  crypto: ^3.0.3
  meta: ^1.10.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
