name: futures_position_monitor
description: CLI example for monitoring futures positions.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  binance_core:
    path: ../../packages/binance_core
  binance_futures:
    path: ../../packages/binance_futures
