# PROTECTED: No cambiar versiones ni flujos de CI sin justificacion en PR_JUSTIFICATION.md
name: futures_position_monitor
description: CLI example for monitoring futures positions.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.11.5 <4.0.0"

dependencies:
  ash_binance_api_core: ^0.1.1+2
  ash_binance_api_futures: any
dev_dependencies:
  lints: ^5.0.0
