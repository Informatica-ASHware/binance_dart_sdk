# PROTECTED: No cambiar versiones ni flujos de CI sin justificacion en PR_JUSTIFICATION.md
name: margin_cross_trade
description: CLI example for a margin trading workflow.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.11.5 <4.0.0"

dependencies:
  ash_binance_api_core: ^0.1.1+3
  ash_binance_api_spot: ^0.1.0+1
  ash_binance_api_margin: ^0.1.0+1
dev_dependencies:
  lints: ^5.0.0
